---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: k8sibleworkflows.k8sible.core.k8sible.io
spec:
  group: k8sible.core.k8sible.io
  names:
    kind: K8sibleWorkflow
    listKind: K8sibleWorkflowList
    plural: k8sibleworkflows
    singular: k8sibleworkflow
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.source.repository
      name: Repository
      type: string
    - jsonPath: .status.pendingPlaybooks
      name: Pending
      type: string
    - jsonPath: .status.lastSuccessfulRun.endTime
      name: Last Success
      type: date
    - jsonPath: .status.lastFailedRun.endTime
      name: Last Failure
      type: date
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: K8sibleWorkflow is the Schema for the k8sibleworkflows API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: K8sibleWorkflowSpec defines the desired state of K8sibleWorkflow
            properties:
              apply:
                description: Apply defines the apply playbook configuration
                properties:
                  maxRetries:
                    default: 3
                    description: MaxRetries is the maximum number of retry attempts
                      for failed jobs
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                  path:
                    description: Path is the path to the playbook file in the repository
                    type: string
                  schedule:
                    description: Schedule is an optional cron schedule for running
                      the playbook
                    type: string
                required:
                - path
                type: object
              configMapRef:
                description: |-
                  ConfigMapRef references a configmap whose keys will be mounted as environment variables
                  in the ansible job pod
                properties:
                  name:
                    description: Name is the name of the configmap
                    type: string
                required:
                - name
                type: object
              failureCycleCooldown:
                description: |-
                  FailureCycleCooldown is the duration to wait before retrying after an apply failure.
                  If not set, retries happen immediately.
                  Examples: "1h", "30m", "24h"
                type: string
              reconcile:
                description: Reconcile defines the optional reconcile playbook configuration
                properties:
                  maxRetries:
                    default: 3
                    description: MaxRetries is the maximum number of retry attempts
                      for failed jobs
                    format: int32
                    maximum: 10
                    minimum: 0
                    type: integer
                  path:
                    description: Path is the path to the playbook file in the repository
                    type: string
                  schedule:
                    description: Schedule is an optional cron schedule for running
                      the playbook
                    type: string
                required:
                - path
                type: object
              secretRef:
                description: |-
                  SecretRef references a secret whose keys will be mounted as environment variables
                  in the ansible job pod
                properties:
                  name:
                    description: Name is the name of the secret
                    type: string
                required:
                - name
                type: object
              source:
                description: Source defines the git repository source
                properties:
                  reference:
                    description: Reference is the git reference (branch, tag, or commit)
                    type: string
                  repository:
                    description: Repository is the URL of the git repository
                    type: string
                  secretRef:
                    description: SecretRef references a secret containing the git
                      credentials
                    properties:
                      key:
                        default: token
                        description: Key is the key in the secret containing the token
                          (defaults to "token")
                        type: string
                      name:
                        description: Name is the name of the secret
                        type: string
                    required:
                    - name
                    type: object
                required:
                - repository
                type: object
            required:
            - apply
            - source
            type: object
          status:
            description: K8sibleWorkflowStatus defines the observed state of K8sibleWorkflow.
            properties:
              applyCommit:
                description: ApplyCommit tracks the last seen commit for the apply
                  playbook
                properties:
                  date:
                    description: Date is when the commit was authored
                    format: date-time
                    type: string
                  message:
                    description: Message is the commit message (truncated)
                    type: string
                  sha:
                    description: SHA is the commit SHA
                    type: string
                type: object
              applyScheduleStatus:
                description: ApplyScheduleStatus tracks the apply playbook schedule
                properties:
                  lastScheduledTime:
                    description: LastScheduledTime is when the playbook was last scheduled
                      to run
                    format: date-time
                    type: string
                type: object
              conditions:
                description: Conditions represent the current state of the K8sibleWorkflow
                  resource.
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              lastFailedRun:
                description: LastFailedRun contains information about the last failed
                  playbook run
                properties:
                  endTime:
                    description: EndTime is when the job ended
                    format: date-time
                    type: string
                  message:
                    description: Message contains additional information about the
                      run
                    type: string
                  startTime:
                    description: StartTime is when the job started
                    format: date-time
                    type: string
                  succeeded:
                    description: Succeeded indicates if the job succeeded
                    type: boolean
                  type:
                    description: Type is the playbook type (apply or reconcile)
                    type: string
                required:
                - succeeded
                - type
                type: object
              lastSuccessfulRun:
                description: LastSuccessfulRun contains information about the last
                  successful playbook run
                properties:
                  endTime:
                    description: EndTime is when the job ended
                    format: date-time
                    type: string
                  message:
                    description: Message contains additional information about the
                      run
                    type: string
                  startTime:
                    description: StartTime is when the job started
                    format: date-time
                    type: string
                  succeeded:
                    description: Succeeded indicates if the job succeeded
                    type: boolean
                  type:
                    description: Type is the playbook type (apply or reconcile)
                    type: string
                required:
                - succeeded
                - type
                type: object
              lastTriggerReason:
                description: |-
                  LastTriggerReason indicates why the pending playbooks were queued
                  Values: "new_commit", "schedule", "failure_retry"
                type: string
              pendingPlaybooks:
                description: PendingPlaybooks is a list of playbook types waiting
                  to be executed
                items:
                  type: string
                type: array
              reconcileCommit:
                description: ReconcileCommit tracks the last seen commit for the reconcile
                  playbook
                properties:
                  date:
                    description: Date is when the commit was authored
                    format: date-time
                    type: string
                  message:
                    description: Message is the commit message (truncated)
                    type: string
                  sha:
                    description: SHA is the commit SHA
                    type: string
                type: object
              reconcileScheduleStatus:
                description: ReconcileScheduleStatus tracks the reconcile playbook
                  schedule
                properties:
                  lastScheduledTime:
                    description: LastScheduledTime is when the playbook was last scheduled
                      to run
                    format: date-time
                    type: string
                type: object
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
